import styled from '@emotion/styled'

export const Wrapper = styled('div')({
  padding: '1.5rem 0',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',

  a: {
    color: '#212121',
  },
})
